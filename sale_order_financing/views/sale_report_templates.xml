<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="financing_template" inherit_id="sale.report_saleorder_document" name="Financing">
        <xpath expr="//table[hasclass('o_main_table')]" position="replace">
            <table class="table table-sm o_main_table">
                <!-- In case we want to repeat the header, remove "display: table-row-group" -->
                <thead style="display: table-row-group">
                    <tr>
                        <th name="th_description" class="text-left">Description</th>
                        <th name="th_subtotal" class="text-right">
                            <span groups="account.group_show_line_subtotals_tax_excluded">Price</span>
                            <span groups="account.group_show_line_subtotals_tax_included">Price</span>
                        </th>
                    </tr>
                </thead>
                <tbody class="sale_tbody">

                    <t t-set="current_subtotal" t-value="0"/>

                    <t t-foreach="doc.order_line" t-as="line">

                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

                        <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                            <t t-if="not line.display_type">
                                <td name="td_name"><span t-field="line.name"/></td>
                                <td name="td_subtotal" class="text-right o_price_total">
                                    <span t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                    <span t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                </td>
                            </t>
                            <t t-if="line.display_type == 'line_section'">
                                <td name="td_section_line" colspan="99">
                                    <span t-field="line.name"/>
                                </td>
                                <t t-set="current_section" t-value="line"/>
                                <t t-set="current_subtotal" t-value="0"/>
                            </t>
                            <t t-if="line.display_type == 'line_note'">
                                <td name="td_note_line" colspan="99">
                                    <span t-field="line.name"/>
                                </td>
                            </t>
                        </tr>

                        <t t-if="current_section and (line_last or doc.order_line[line_index+1].display_type == 'line_section')">
                            <tr class="is-subtotal text-right">
                                <td name="td_section_subtotal" colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span
                                        t-esc="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": doc.pricelist_id.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </t>
                </tbody>
            </table>
        </xpath>
        <xpath expr="//div[@name='so_total_summary']" position="after">
            <t t-if="doc.financing">
                <br role="presentation"></br>
                <h2 class="mt40">
                    <span>Financing details </span>
                </h2>
                <div class="row mt32 mb32" id="financing_informations">
                    <div class="col-auto col-3 mw-100 mb-2">
                        <strong>Bank:</strong>
                        <p class="m-0" t-field="doc.bank_id"/>
                    </div>
                    <div class="col-auto col-3 mw-100 mb-2">
                        <strong>Month duration:</strong>
                        <p class="m-0">
                            <span t-field="doc.month_duration"></span>
                            <span> months</span>
                        </p>
                    </div>
                    <div class="col-auto col-3 mw-100 mb-2">
                        <strong>Interest:</strong>
                        <p class="m-0">
                            <span t-esc="doc.interest * 100"></span>
                            <span> %</span>
                            <span t-field="doc.financing_type"/>
                        </p>
                    </div>
                </div>
                <table class="table table-sm o_main_table">
                    <thead style="display: table-row-group">
                        <tr>
                            <th name="th_amount_total" class="text-left">Amount to financed</th>
                            <th name="th_initial_amount" class="text-left">Initial payment</th>
                            <th name="th_monthly_fee" class="text-right">Monthly payment</th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">
                        <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                            <td name="td_amount_total">
                                <span t-field="doc.amount_total"/>
                            </td>
                            <td name="td_initial_amount" class="text-left o_price_total">
                                <span t-field="doc.initial_amount"/>
                            </td>
                            <td name="td_monthly_fee" class="text-right o_price_total">
                                <span t-field="doc.monthly_fee"/>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </t>
        </xpath>
    </template>
</odoo>
